#include <stdio.h>

int main(int argc, char* argv[]){
    //imprimir numeros de 0 a100 usando while

    int contador = 0;
  while(contador <= 100) {
    printf("contador %d\n", contador);
    contador++; 
  }
return 0;
}